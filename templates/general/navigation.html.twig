
{% if app.user %}
<nav class="d-flex-center p-3">
    <a class="m-1" href="{{ path('app_home') }}">Home</a>
    <a class="m-1" href="{{ path('app_home') }}">Home</a>


    <a class="ml-auto d-flex" href="{{ path('app_logout') }}">Logout</a>
</nav>
{% else %}
    <nav>
        <ul>
            <li><a href="{{ path('app_login') }}">Login</a></li>
            <li><a href="{{ path('app_register') }}">Register</a></li>
        </ul>
    </nav>
{% endif %}